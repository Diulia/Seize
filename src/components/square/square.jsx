import React from "react";

export const Square = ({Children}) => {
  const style = {
    width: "90px",
    height: "112px",
    display: "flex",
    backgroundColor: "#5B9AA0",
  };

  return (
    <div style={style}>
        <div></div>

    </div>
  );
};
